import defineMessages from '../../define-messages.macro';

defineMessages.setupPrefix('components.some-component');
defineMessages.setupPrefix('components.some-other-component');

const messages = defineMessages({
    greeting: 'hello'
});
